import styled from 'styled-components';

const Wrapper = styled.main `
nav {
  display: flex;
  align-items: center;
}
  h1 {
    font-weight: 700;
    span {
      color: darkgray;
    }
  }
  p {
    color: grey;
  }
`

export default Wrapper;
